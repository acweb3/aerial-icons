import { AerialIcon } from "components/AerialIcon";
import { Copy } from "components/Copy";
import { Mint } from "components/Mint";
import { SiteWrapper } from "components/SiteWrapper";

export const App = () => {
	return (
		<SiteWrapper>
			<AerialIcon />
			<Copy />
			<Mint />
		</SiteWrapper>
	);
};
