import styled from "styled-components";

export const AerialIconImage = styled.img``;

export const AerialIconContent = styled.div`
    width: 70%;
`;

export const AerialIcon = styled.div`
    position: absolute;
    top: 0;
    left: 0;
    height: 400px;
    width: 100%;

    display: flex;
    justify-content: center;

    z-index: 0;
`;
